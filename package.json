{
  "name": "ai-summary-helper",
  "version": "2.0.313",
  "description": "Multi-platform build tooling for the AI Summary Helper WebExtension. src/ is the single source of truth; scripts/build.js syncs it to per-platform targets.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "build:chrome": "node scripts/build.js chrome",
    "build:firefox": "node scripts/build.js firefox",
    "build:ios": "node scripts/build.js ios",
    "build:clean": "node scripts/build.js --clean",
    "build:all": "node scripts/build.js --clean"
  },
  "engines": {
    "node": ">=18"
  }
}